kubernetes:
  namespace: polaris-icons-production
  context: tier4-us-east1-2
ci:
  require:
    - buildkite/polaris-icons
    - buildkite/polaris-icons-production-builder
    - buildkite/shopify-polaris-icons-k8s-template-validator
dependencies:
  override:
    - echo "No dependency installation required"
